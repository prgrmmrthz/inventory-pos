import { MyEventListenerDirective } from './my-event-listener.directive';

describe('MyEventListenerDirective', () => {
  it('should create an instance', () => {
    const directive = new MyEventListenerDirective();
    expect(directive).toBeTruthy();
  });
});
